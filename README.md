# box-shadow-generator

Build box-shadow CSS with multiple layers.

Each layer has its own X/Y offset, blur, spread, color, and inset toggle. Stack layers to create complex shadows. Preview on a card and copy the CSS.

## Run

```bash
npm install && npm run dev
```
